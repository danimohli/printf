#include <stdlib.h>
#include <stdarg.h>
/**
 * _printf - similar to printf
 * @format: constant format
 * Return: len of format
 */
int _printf(const char *format, ...)
{
	while (format != '\0')
	{
		if ()
	}
}
